<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher123_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher123_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DonateLifeApplication">
        <activity android:name=".UserLogin"></activity>
        <activity android:name=".SubmissionSuccess" />
        <activity android:name=".SearchSQLiteActivity" />
        <activity android:name=".Search1Activity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ResetActivity" />
        <activity android:name=".PasswordActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Donate" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>